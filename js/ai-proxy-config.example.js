/* Example:
window.AI_PROXY_CONFIG = Object.freeze({
  baseUrl: "https://your-ai-proxy.example.com/api/ai",
  timeoutMs: 15000,
});
*/
window.AI_PROXY_CONFIG = Object.freeze({
  baseUrl: "",
  timeoutMs: 22000,
});
