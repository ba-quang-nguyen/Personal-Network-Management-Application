/* js/google-maps-config.example.js — copy thành google-maps-config.js rồi điền key của bạn.
   mapId: để "DEMO_MAP_ID" là chạy ngay; đổi thành Map ID riêng nếu muốn custom style.
   KHÔNG để mapId trống — thiếu Map ID sẽ gây lỗi "This page can't load Google Maps correctly".
*/
window.GOOGLE_MAPS_CONFIG = Object.freeze({
  apiKey: "YOUR_BROWSER_RESTRICTED_GOOGLE_MAPS_KEY",
  mapId: "DEMO_MAP_ID",
});
